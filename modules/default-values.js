//DEFAULT VALUES:
const minRows = 1;
const maxRows = 99;
const defaultRows = 6;
const minCols = 3;
const maxCols = 8;
const defaultCols = 5;

export { minRows, maxRows, defaultRows, minCols, maxCols, defaultCols }